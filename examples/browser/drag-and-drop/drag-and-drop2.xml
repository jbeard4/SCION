
<scxml 
    xmlns="http://www.w3.org/2005/07/scxml"
    version="1.0"
    profile="ecmascript">

    <datamodel>
        <data id="firstEvent"/>
        <data id="eventStamp"/>
        <data id="rectNode"/>
        <data id="rectX"/>
        <data id="rectY"/>
    </datamodel>

    <script>
      function parseStyle(str){
        return str ? parseInt(str.match(/(\d+)px/)[1]) : 0;
      }
    </script>

    <state id="idle">
        <onentry>
            <assign location="rectNode" expr="null"/>
            <assign location="rectX" expr="null"/>
            <assign location="rectY" expr="null"/>
        </onentry>

        <transition event="mousedown" target="dragging">
            <assign location="rectNode" expr="_event.data.target"/>
            <assign location="rectX" expr="parseStyle(rectNode.style.left)"/>
            <assign location="rectY" expr="parseStyle(rectNode.style.top)"/>
            <assign location="firstEvent" expr="_event.data"/>
            <assign location="eventStamp" expr="_event.data"/>
        </transition>
    </state>

    <state id="dragging">

        <transition event="mouseup" target="idle"/>

        <transition event="mousemove" target="dragging">
            <script>
                var dx = eventStamp.clientX - _event.data.clientX;
                var dy = eventStamp.clientY - _event.data.clientY;

                //note that rectNode, rectX and rectY are all exposed
                //from the datamodel as local variables
                rectNode.style.left = rectX -= dx;
                rectNode.style.top = rectY -= dy;
            </script>
            <assign location="eventStamp" expr="_event.data"/>
        </transition>
    </state>

</scxml>
